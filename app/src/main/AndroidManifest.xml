<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.barbershop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Barbershop"
        android:usesCleartextTraffic="true">
        <activity android:name=".SelectDateActivity"></activity>
        <activity android:name=".SelectBarberActivity" />
        <activity android:name=".SelectServiceActivity" />
        <activity android:name=".ShopPageActivity" />
        <activity android:name=".MyAccountActivity" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".SigninActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>